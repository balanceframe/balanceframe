{
  "name": "@balanceframe/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BalanceFrame web frontend — responsive review surface/controller for transaction categorization",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@balanceframe/workflow-store": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  }
}
