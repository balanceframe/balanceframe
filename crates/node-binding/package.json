{
  "name": "@balanceframe/native",
  "version": "0.1.0",
  "description": "N-API bindings for BalanceFrame Rust financial core",
  "napi": {
    "name": "balanceframe",
    "triples": {
      "defaults": true,
      "additional": [
        "x86_64-unknown-linux-gnu",
        "aarch64-unknown-linux-gnu"
      ]
    }
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.0.0"
  },
  "scripts": {
    "build": "napi build --release"
  }
}
