{
  "name": "@balanceframe/actual-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Actual Budget API adapter for BalanceFrame — read-only gateway with Observe-only mode",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./connector": "./dist/connector.js",
    "./types": "./dist/types.js",
    "./normalizer": "./dist/normalizer.js",
    "./credentials": "./dist/credentials.js"
  },
  "scripts": {
    "build": "tsc",
    "coverage": "vitest run --coverage",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@actual-app/api": "^26.7.0",
    "@balanceframe/protocol-generated": "workspace:*",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "@actual-app/core": "^26.7.0",
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^1.0.0",
    "vitest": "^1.0.0"
  }
}
